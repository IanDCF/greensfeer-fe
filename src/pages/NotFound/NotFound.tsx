const NotFound = () => {
  return (
    <div
      style={{
        fontSize: "4rem",
        display: "flex",
        justifyContent: "center",
        marginTop: "15rem",
      }}
    >
      Not Found
    </div>
  );
};

export default NotFound;
